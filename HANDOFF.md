# HANDOFF

最終更新: 2026-08-14

## 1. 完了した作業

- **Phase 0（基盤）完了・タグ`phase-0`push済み**: Vite+TS+Phaser3、固定タイムステップループ、決定論ユーティリティ、入力抽象化、縦スクロールカメラ、レーダー
- **Phase 1（ボールと1選手）完了・タグ`phase-1`push済み**: ドリブル・キック(速度軸+弾道軸)・ロングドリブル・ボール物理(重力/バウンド/転がり摩擦/疑似3D高さ)・カメラのボール追従切替・表示不具合(キャンバス中央寄せ)修正
- **Phase 2（11 vs 11とチームAI）完了・タグ`phase-2`push済み**: `GameState.players[]`(22人配列化)、フォーメーション定義(4-4-2/4-3-3/3-5-2/5-3-2)、非操作選手AI(ホーム復元力+ボール引力+オフサイド意識の重み付き合成、sqrtを使わず内積argmaxで8方向量子化)、カーソル切替+カーソルパス(Yボタンの文脈依存解決)、キーパーAI(自動/手動交代・キャッチ/パンチング)、スライディングタックル(Bボタンの文脈的再利用、ファールなし)、決定論/パフォーマンス強化(InputManagerの潜在バグ修正・22人版決定論回帰テスト)
- テスト140件・`check:determinism`・`build`すべてgreen。`main`は`origin/main`と同期済み(`2ac7823`)、タグ`phase-0`/`phase-1`/`phase-2`すべてpush済み
- 開発サーバー起動を確認(`npm run dev`、http://localhost:5173、サーバーログにエラーなし)。**このセッションもBrowser paneのスクリーンショットが取得できなかったため、実際の見た目はユーザー側で目視確認中**(未確認のまま次のタスクへ)

## 2. 進行中の作業と正確な現状

- Phase 0〜2はすべて完了・タグ付け・push済みでクリーンな状態。未コミットの変更なし。
- ユーザーがブラウザでPhase 2の見た目を確認中（結果待ち）。
- **これから Phase 3（試合として成立させる）の実装計画をPlan Modeで作成する**（本HANDOFF更新の直後に着手）。Phase 3スコープ: ゴール描画+得点判定、ルール実装(スローイン/ゴールキック/コーナーキック/オフサイド判定のON-OFF切替)、前後半+試合時間+スコア表示(後半サイドチェンジ)、CPU(Team B)の自律的攻撃行動(シュート/パス/崩し、難易度3段階)、リプレイ用入力ログ記録、効果音(素材が無ければフックのみ)、決定論/60fps維持の方針。

## 3. 次にやるべきこと（優先順）

1. Phase 3実装計画（Plan Mode）を作成し、ユーザー承認を得る
2. 承認後、Phase 3実装（規模次第で副マイルストーン分割、Phase 2と同様の進め方を想定）
3. **ユーザーからのPhase 2見た目フィードバックを計画/実装に反映**（特にYボタンの文脈依存解決とタックルのBボタン割り当てへの違和感有無）
4. ゲームパッド実機確認（Phase 0から継続保留、ユーザー側でパッド入手次第）
5. Phase 1由来の精密照準軸・カーブ/バックスピンの実装タイミング検討（未着手のまま持ち越し）

## 4. 未解決の課題・ユーザー判断待ち

- **Phase 2の見た目が未確認**（本セッション時点）。ユーザーの目視確認結果を待っている
- **Yボタンの文脈依存解決**（`sim/cursor.ts`）・**Bボタンのタックルへの文脈的再利用**（`sim/tackle.ts`）: CLAUDE.mdの記述を独自に解釈して実装した箇所。プレイして違和感が無いか要フィードバック
- 非操作選手AI（Team B含む）は自律的にキック/パス/タックルを行わない設計（Phase 2はポジショニングAIのみ）。Phase 3で計画する「CPU自律攻撃行動」がこのギャップを埋める予定
- ゲームパッド実機確認が継続的に未実施（Phase 0から持ち越し。SFC論理ボタンX→Xbox Y割り当て等）
- Phase 1で対象外とした精密照準軸・カーブ/バックスピンの実装タイミング未決定
- Phase 2の新規定数(フォーメーション座標・AI重み・ヒステリシス幅・タックルのタイミング等)はすべて仮値、プレイテストでの調整が必要

## 5. 重要な決定事項と理由

- Phase 0〜2それぞれの詳細な設計判断は各コミットメッセージ（`git log`）に集約済み。要点はSection 1参照
- **players[]の固定index規約**（0-10=TeamA、11-21=TeamB、GKは各チーム先頭）と、それに基づく「昇順走査+厳密な`<`比較で小さいindexが勝つ」タイブレークは、Phase 3以降のルール実装（オフサイド判定、CPU意思決定等）でも踏襲すべき決定論パターンとして確立済み
- `Inputs`型を変更せず、tickをまたぐ状態はすべて`GameState`側（各選手のフィールドやトップレベルフィールド）に持たせる方針は、Phase 3のリプレイ用入力ログ設計にも直結する（`GameState`が決定論的なら、入力列だけで完全に再生可能という前提が既に成立している）

## 6. 変更ファイル一覧（このセッション）

コードの変更は無し。開発サーバーの起動確認と本ファイルの更新のみ。

## 申し送り

- モデル設定は opusplan。設計判断を伴うタスクは必ず Plan Mode（Shift+Tab）で開始する。
- リポジトリ: `origin` = `https://github.com/appstockhub/vertical-striker.git`。`main`ブランチに直接コミットする運用（ユーザー承認済み）。
- タグ: `phase-0`・`phase-1`・`phase-2`すべて作成・push済み。
- **Browser pane（サンドボックス内蔵ブラウザ）は3セッション連続でスクリーンショットが撮れない状態が続いている**（`document.hidden`が常にtrue）。次回セッションでもまずスクリーンショットを試すが、ダメな場合は無理に検証せず「未確認」と正直に報告し、ユーザーの目視確認に委ねること。代替検証手段: `canvas.getContext('2d').getImageData(...)`のピクセルヒストグラム、コンソールエラー監視、型チェック/テスト/ビルドのgreen確認。
- Phase 2実装は`.claude/plans/claude-md-cheerful-cookie.md`の計画書に沿って実施済み（同ファイルは次のPlan Mode呼び出しでPhase 3の内容に上書きされる想定）。
