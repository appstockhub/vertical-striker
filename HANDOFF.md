# HANDOFF

最終更新: 2026-08-14

## 1. 完了した作業

- **Phase 0（基盤）完了・タグ`phase-0`push済み**: Vite+TS+Phaser3スキャフォールド、固定タイムステップループ、決定論ユーティリティ(固定小数点/mulberry32)、入力抽象化(Gamepad/Keyboard→SFC論理ボタン+8方向)、縦スクロールカメラ、レーダー骨格
- **Phase 1（ボールと1選手）完了・タグ`phase-1`push済み**: ドリブル（吸着させず触れると転がる）・キック（速度軸+弾道軸、精密照準/カーブは対象外）・ロングドリブル(L/R)・ボール物理（重力・バウンド・転がり摩擦・疑似3D高さ）・カメラのボール追従切替。テスト62件、`check:determinism`・`build`すべて成功
- **表示不具合の修正**: `#game-root`のCSSサイズ未指定が原因でキャンバスが画面中央でなく右寄りに表示される不具合を修正（`width/height:100%`指定）。選手/ボールを拡大し配色を調整（プレイヤー橙赤・ボール白、縁取り追加）
- `main`ブランチへコミット済み、`origin`へpush済み（`720f92c`まで）。タグ`phase-0`・`phase-1`ともにpush済み

## 2. 進行中の作業と正確な現状

- Phase 0・Phase 1ともに完了・リモート反映済み。未コミットの変更なし。
- **これから Phase 2（11 vs 11とチームAI）の実装計画をPlan Modeで作成する**（本HANDOFF更新の直後に着手）。
- **現在ゲームパッドが手元に無いため、Phase 2の動作確認はキーボードのみで行う**。パッド固有の検証（接続オーバーレイ、B/Y/A/X/L/R配置、アナログスティックのデッドゾーン等）は保留のまま。

## 3. 次にやるべきこと（優先順）

1. Phase 2実装計画（Plan Mode）: フォーメーション定義(4-4-2/4-3-3/3-5-2/5-3-2)、非操作選手AI(ホームポジション復元力+ボール引力+オフサイドライン意識の重み付きベクトル合成)、カーソル切替+カーソルパス(Yボタン、受け手マーカー表示)、キーパーAI、スライディングタックル(ファール無し)、22人分の決定論維持、60fps維持のパフォーマンス方針、を計画に含める
2. 計画承認後、Phase 2実装
3. **ユーザー側でのゲームパッド実機確認**（下記4参照、Phase 0から持ち越し継続）
4. Phase 1で対象外とした精密照準軸・カーブ/バックスピンをどのタイミングで実装するか要相談（CLAUDE.mdの「操作仕様」は確定仕様のため、いずれか要実装）

## 4. 未解決の課題・ユーザー判断待ち

- **ゲームパッド実機確認が継続的に未実施**（Phase 0から持ち越し）。特にSFC論理ボタンX→Xbox Y割り当て（対称性からの推測、要目視確認）。**現在ユーザー手元にパッドが無いため、当面はキーボードのみでの検証を継続する運用**とする
- 精密照準軸(L/R+キック)・カーブ/バックスピン(キック後入力)の実装タイミング未決定（Phase 1で意図的に対象外とした、CLAUDE.mdの「操作仕様」は確定仕様として実装必須）
- Phase 1の新規物理定数（`src/sim/ballConstants.ts`）はすべて仮値。実機/プレイテストでの調整が必要
- Phase 0由来の仮値（ピッチ寸法、カメラ先読み定数、レーダー配置等）も同様に未調整のまま

## 5. 重要な決定事項と理由

- **キーボードのSFC論理ボタン仮割り当て**（`src/input/keyboard.ts`）: 移動=矢印キー/WASD、B=Z、A=X、Y=C、X=V、L=Q、R=E。ロングドリブルはQ/Eをボール密着中に押しっぱなし
- Phase 0/1の設計判断（Fixed型による決定論、`Inputs`型を拡張せずGameState側にtickをまたぐ状態を持たせる方針、ドリブルタッチの上書き方式、重力の空中判定ゲート等）は過去のHANDOFFエントリ参照（git履歴・コミットメッセージに集約済み）
- **Phase 2の検証方針**: ユーザー手元にゲームパッドが無い期間はキーボードのみで動作確認を行う。パッド固有の挙動確認は都度HANDOFFに保留事項として明記し続ける

## 6. 変更ファイル一覧（このセッション）

タグ`phase-1`の作成・push、`main`のpush（コード変更は前セッションターンまでに完了済み、本ターンはリモート反映と本ファイルの更新のみ）。

## 申し送り

- モデル設定は opusplan。設計判断を伴うタスクは必ず Plan Mode（Shift+Tab）で開始する。
- リポジトリ: `origin` = `https://github.com/appstockhub/vertical-striker.git`。`main`ブランチに直接コミットする運用（ユーザー承認済み）。
- タグ: `phase-0`・`phase-1`ともに作成・push済み。
- 前回セッションのBrowser pane（サンドボックス内蔵ブラウザ）は終始コンポジット/表示不可の状態だった。次回セッションではまずスクリーンショットが正常に撮れるか試すこと。
