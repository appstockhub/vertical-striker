# HANDOFF

最終更新: 2026-08-15 (7周目: リスタート猶予+イベントバナー実装。GitHub Pages公開が確認された)

## 0-0000. 7周目 (最新): リスタート猶予 + イベントバナー (5周目実プレイ確認のフィードバック対応)

5周目(マーク+サポートラン)を実プレイ確認したユーザーから3件のフィードバックがあり、対応した
(コミット`4fb948c`、push済み。**まだユーザーのブラウザ実プレイ確認を経ていない**)。

**5周目の効果自体は確認できた**: サポートラン「少し効いている」、マーク「効いている」、
カーソルの暴れ「多少解消した」。その上で新たに3点報告された。

Plan Modeで事前に「数値調整で対応できるもの」と「構造的な変更が必要なもの」を切り分けて
提示・承認を得たうえで実装（計画ファイルは使い捨てのため`HANDOFF.md`にのみ記録）。

1. **リスタート猶予**(構造変更): 「キックオフ/ゴールキックのタイミングでなぜか敵側が
   先に蹴れる」の対応。根本原因: キックオフはFW同士がボールから完全に対称な距離
   (F442で153px)に位置するが、`lastTouchTeam`/`linePossessionTeam`がnull(競り合い扱い)
   になり両チームに全く同じ強さ(weight 3.0、リーシュ免除)の追跡権が与えられる。AIは
   反応レイテンシがゼロだが人間には現実的な反応時間があるため、対称なはずの距離が
   実質AI有利に倒れていた。`GameState`に`restartGraceTeam`/`restartGraceTicksLeft`を新設し、
   `computeChaseRightIndices`(`src/sim/teamAI.ts`)に`suppressedTeam`引数を追加
   (デフォルトnullで既存呼び出し無変更)。リスタート直後の短い間、相手チームの追跡権を
   丸ごとゼロにする。キックオフ(試合開始/得点後/前後半切替)とゴールキック/スローイン/
   コーナーそれぞれで猶予チームを決定。既存のゴールキック時`lastTouchTeam`振り分け
   (4周目追加)は、primaryの引力weightが保持/守備の区別と無関係に同じ値であるため
   実効性が薄いことが判明したが、他の消費先(コーナー判定・Kick音)があるため削除せず残した。
   定数: `KICKOFF_GRACE_TICKS=15`、`RESTART_GRACE_TICKS=63`(既存のゴールキック190pxバッファ
   ÷3px/tickと一致)。
2. **イベントバナー**(構造変更、小規模): 「スローインの概念がない」「キーパーがキャッチする
   概念がない」の対応。ユーザーに確認した結果、**試合を止めずにバナー+効果音のみ**の方針
   (既存Phase 3の「非得点の境界復帰は試合停止の演出を持たない」方針を維持)。両方とも
   実装は正しく動作していたが、画面上・音声上他の瞬間と見分ける手がかりが無く
   「起きていない」ように見えていた。`GameState.lastEvent`(`lastTouchTeam`等と同じ
   echoフィールド、物理/AIには影響しない)を新設し、境界イベント発生時とGKの真のキャッチ
   (`secured`、人間操作・CPU自動セーブ両方)で記録。新規`src/render/eventBanner.ts`
   (`scoreboard.ts`と同じ純関数パターン)で0.75秒間のHUDバナーを`PitchScene.ts`に統合。
   `SoundEventId.GkCatch`も追加(音声アセット自体は未調達のためフックのみ)。
3. **検証中に発見した副作用**: `suppressedTeam`導入のバタフライ効果(トラジェクトリが
   わずかに変わることで、以前踏んでいなかった経路上のexact tieに遭遇する)で観戦
   シミュレーターの振動検出が新たな偽陽性を複数回検出。ball deadzoneの単純な拡大では
   whack-a-mole的に別の場所で振動が再発したため、より原因に即した2つの修正で収束させた:
   primary追跡者専用のボールデッドゾーンを4px→9pxに拡大(既にボールを保持している味方へ
   無意味に収束しようとする往復を解消。cover/非追跡権には影響させない)、追跡権
   メンバーシップの距離バケットを48px→64pxに拡大(ゴール前混戦でのメンバー入れ替わり
   チャタリングを解消)。いずれも観戦シミュレーター全マトリクス(8試合)で団子度等の
   副作用が無いことを確認済み。
4. **検証**: テスト**314件**・`check:determinism`・`sim:sanity`全10基準・`build`すべてgreen。
   新規テスト: `tests/sim/restartGrace.test.ts`(猶予チーム決定ロジック+バグ回帰テスト)、
   `tests/render/eventBanner.test.ts`(純関数テスト)、`tests/sim/teamAI.test.ts`/
   `tests/render/soundEvents.test.ts`に追加分。
5. **GitHub Pagesデプロイの確認**: 6周目でユーザーが「できてるはず」と報告した際に
   実際は失敗していた実行(`902cf8c`、Pages未有効化時点のもの)を、ユーザーがSettings変更後に
   再実行して**成功**していたことをAPI経由(`gh`未インストールのため`curl`で代替)で確認。
   さらに本コミット(`4fb948c`)のpushでも新規デプロイが走り**成功**、
   `https://appstockhub.github.io/vertical-striker/`が実際にHTTP 200で応答することを確認した。
   **GitHub Pages公開は完了**（6周目の残タスクは解消）。

## 0-000. 6周目: GitHub Actions による GitHub Pages 自動デプロイ設定

ユーザー指示「実プレイ確認が必要な調整は後回しにして、確認なしで進められる積み残しタスクを
やりたい」を受けて着手。Phase 3計画時点から「Phase 1完了時点から必要だったが未実施」と
記録されていた積み残し（セクション1参照）。

- **`.github/workflows/deploy-pages.yml`を新規作成**: `main`へのpushで自動的に
  `npm ci → check:determinism → npm test → npm run build → Pagesへデプロイ`を実行する
  2ジョブ構成（build→upload-pages-artifact、deploy→deploy-pages）。`workflow_dispatch`
  による手動実行にも対応。Node 22（vite 8の要求`^20.19.0 || >=22.12.0`を満たす）。
- **`vite.config.ts`の`base: './'`は既にGitHub Pages対応済みで変更不要だった**
  （Phase 0時点で「Phase 1完了時のデプロイに備えて」設定済みだったコメントが残っていた）。
  ビルド出力(`dist/index.html`)の`<script src>`/`<link href>`が`./assets/...`の相対パスに
  なっていることを確認済み。
- **ビルド成果物の検証**（実プレイではなく静的資産の解決経路のみ）: `npm run build`後、
  `dist/`をGitHub Pagesの実URL構造(`/vertical-striker/`サブパス配下)を模したディレクトリに
  配置し、ローカルHTTPサーバー+Browser paneで読み込みを確認。`location.href`が
  `http://localhost:4173/vertical-striker/`、スクリプト/CSSの実際の解決先が
  `http://localhost:4173/vertical-striker/assets/...`であることを確認、コンソールエラー無し、
  Phaser起動ログのみ、試合前設定オーバーレイ(`match-setup-overlay`)のDOMも正常に描画された。
  これは「サブパス配信でもアセットパスが壊れない」ことの確認であり、ゲームプレイの動作確認
  ではない。
- **ユーザー判断待ちの手動設定が2つあったが、両方完了済み**（7周目で確認、上記0-0000参照）:
  リポジトリのpublic化、Settings > Pages > Source を「GitHub Actions」に設定、
  いずれも完了し`https://appstockhub.github.io/vertical-striker/`が実際に公開されている
  ことをHTTP 200で確認済み。
- テスト297件・`check:determinism`・`build`すべてgreenの状態でコミット
  （ワークフロー自体はコード変更ではないため、このコミットで既存機能への回帰は無い）。

## 0-00. 5周目: Phase 4第1弾 — マーク + サポートラン (連動性の不足への対応)

**重要: この5周目の変更は、コミット`a71f431`・push済みだが、まだユーザーのブラウザ実プレイ確認を
一度も経ていない**。ユーザーから「実プレイ確認は後日行うが、この内容でコミットしてよい」との
明示的な指示があり、その指示に従って確認前にコミット・pushしている（これまでの4周は逆に
「ユーザーの確認を経てからタグ/pushする」運用だったため、この5周目は運用上の例外）。
前回(4周目)の実プレイ確認で挙がった「サッカーとしての連動性の不足」
(セクション3の旧「Phase 4調整候補」参照)のうち、最も本質的とされた「マークとサポートラン
が一切実装されていない」への対応として、Plan Modeで計画を立てたうえで実装した。

- **中核方針**: 既存の8方向量子化+重み合成には新しい力項を追加せず、ホーム復元力が収束する
  **目標点を役割に応じて差し替える**方式(`computeLineAdjustedHomePosition`が確立したパターンの
  踏襲)。これにより既存の振動対策(deadzone/グリッド量子化/リーシュランプ/sticky facing)を
  すべて継承し、過去3周の再発(団子化・振動・凍結)を避けた。
- **マーク** (新規 `src/sim/marking.ts`): 追跡権を持たない守備側DFライン選手に、自陣ゴールから
  450px圏内に侵入した相手を危険度順(深度昇順→index昇順)に1:1で割り当てる(48px²バケット距離
  +index小のタイブレーク)。ボールから120px以内の相手は追跡権保有者の仕事として除外(団子化への
  主防壁)。マークホーム=対象の24px量子化位置からゴール側48pxの点。
- **サポートラン** (新規 `src/sim/supportRun.ts`): フォーメーションごとにdepthFrac最大の3スロット
  (静的、キャリアはteamAI駆動されないため非キャリア2人以上が常に保証される)が、保持中に
  「ボールの量子化深度+180px前方」へ走り込む(団子検出半径150pxの外・カーソルパス射程220pxの内
  になるよう調整済み)。オフサイドは既存のオンサイドクランプが目標を頭打ちにすることで
  追加コード無しで対応。上限1650px(敵ボックス縁)でピッチ外目標を防止。味方64px以内なら
  Xを64pxずらして密集を回避。
- **検証体制**: 観戦シミュレーターに新メトリクス`supportRunnersAvgAhead`/`markDistanceAvgPx`/
  `markedTicksShare`を追加、正常性基準を9→10項目に拡張(基準9=サポートラン機能率、
  基準10=マーク距離帯)、マトリクスを6→8試合に拡張(passHeavy/defensiveの追加シード)。
  全10基準green、8試合を2回実行して出力完全一致(決定論確認)、`check:determinism`/`build`/
  テスト297件すべてgreen。
- **検証ループで発見・修正した実プレイ直結バグ3件** (いずれも今回のマーク/サポート実装が
  誘発したのではなく、既存コードに潜在していたものが新しい負荷で露出した):
  1. **カーソルの毎tickフリップフロップ** (`src/sim/cursor.ts`/`cursorConstants.ts`):
     自動追従の切替ヒステリシスが絶対マージン(5px²)のみで、ボールから遠い時は選手の
     1tick移動量(3px)で二乗距離が大きく変わり全く機能しなかった。守備時にほぼ等距離の
     2人の間でカーソルが毎tick往復し、両者とも実質凍結する現象があった。距離比17%
     (`CURSOR_SWITCH_DOMINANCE_NUM/DEN`)の相対条件を追加して解消。
     **実プレイで「守備時にカーソルが2人の間で暴れて両方動かなくなる」として体感されていた
     可能性が高い、既存不具合の修正**。
  2. **CPU相互パスの永久デッドロック** (`src/sim/cpuAttackAI.ts`): 向かい合う2人のCPUが
     互いにパスし合い、試合が180tick以上完全停止する現象があった(マーク導入でCPU選手の
     位置分布が変わり顕在化)。「直前に自分へボールを渡した選手にはパスしない」ルールで
     構造的に解消(`GameState.lastTouchPlayerIndex`/`prevTouchPlayerIndex`を新設)。
     「前進パスのみ許可」も試したが、CPUがドリブル主体になりすぎて攻撃転換が強くなり
     人間側が窒息するバランス崩れを実測したため不採用、パス頻度を変えない方式を採用。
  3. **サポート目標のピッチ外化/ゴール前密集**: 初期値(前方100px)は団子検出半径(150px)より
     小さくランナー自身が団子度を悪化させた。180pxへ調整+深度上限1650pxを追加して解消。
- **既知の妥協点(次回以降)**: (1) Aのシュートは試合間の偏りが大きい(8試合中3試合は0本、
  合計19本は基準6の合計制で吸収)。攻撃バランス自体の手触り調整はPhase 4後半の課題として
  持ち越し。(2) defensive(seed3)の団子度4.47は基準4.5にほぼ接している。(3) idle時のマーク
  距離315pxは保持スペルが短すぎてサンプル不足(基準対象外の閾値未満)。

## 0-0. 4周目: 観戦シミュレーター+正常性基準による自律検証体制

「モデル修正→ユーザーが実プレイでバグ発見」の繰り返しを断つため、ユーザー指示により
検証体制自体を作り替えた (コミット`65d00c0`):

- **観戦シミュレーター** `tests/sim/matchSimulator.ts`: フルマッチをsimulate()で回して統計を出す。
  人間入力は4パターンの決定論的スクリプト (積極攻撃/パス多用/守備/放置) で模擬。
  `npm run sim:report` で人間可読のサマリ、`npm run sim:sanity` で基準判定。
- **正常性基準9項目** `tests/sim/soccerSanity.test.ts` (回帰テスト兼用): 振動選手ゼロ/
  シュート後の陣形急変なし/団子度≤4.5/プレス存在(<150px)/CPU攻撃成立(シュート5+)/
  人間攻撃可能(合計5+)/支配率15-85%/得点可能性。フルマッチ6試合を毎回実行して判定。
- このループで報告症状2件(シュート後の一斉後退・前線プレス不在)に加え、**9件の未報告バグ**を
  発見・修正した。最重要は「GKが確保した静止ボールを永遠に蹴れない詰み」(セーブ文脈が
  B/Yを乗っ取る実プレイ直結の欠陥)、「AI GKがシュートに全く反応しない」、「CPU GKが
  ボール確保後に永久フリーズ」、「ゴールキックへの相手プレス待ち伏せループ」。
  詳細はコミットメッセージ参照。
- 実測値 (最終): postShotRetreat 66px→0px、プレス距離 378-627px→35-135px、
  団子度 4.4-5.3→3.4-4.1、振動選手 22人→0人 (基準マトリクス全6試合)。
- **既知の妥協点 (Phase 4持ち越し)**: (1) 団子度基準はユーザー例の3人ではなく4.5
  (保持者+プレス2人+操作選手で構造的に3-4人になるため。3人以下を目指すなら追跡権の
  再設計が必要)。(2) スクリプト人間のシュートは1試合0-4本でユーザー例の5本に届かない
  (A側の攻撃のしやすさ=プレス強度バランスはPhase 4の手触り調整対象)。(3) 振動検出は
  しきい値際の偽陽性が稀にある (基準マトリクス外のシードで1件確認、活発にプレー中の
  CPUキャリアがpath 126/bbox 23.3で境界ぎりぎり検出)。
- **この4周目の変更もまだユーザーのブラウザ実プレイ確認を経ていない**。

## 0-1. 3周目の追記: 団子サッカー + 実プレイ限定の凍結バグ3つを修正

2周目の修正後、ユーザーから「Team Bが依然として前進しない」「ほぼ全員がボールを追いかける
団子サッカーになった」と報告。調査の結果:

- **団子サッカー**: ユーザー提案どおり「ボール追跡権」(`computeChaseRightIndices`、各チーム
  最寄り+カバー1人だけがボールへのフル引力を持つ)を実装して解消。
- **前進しない問題の真因**: 無人(入力None固定)の統合テストでは踏めていなかった、
  「人間が実際に操作している時にのみ」顕在化する3つの独立したバグが原因と判明:
  1. **キック溜め中のカーソル永久固定 (最重要)**: ボール保持中にBを押した直後にボールを
     失うと、kickChargeFramesが非ゼロのまま二度と0に戻らず、無関係な選手にカーソルが
     永久固定されて二度とボールへ追従できなくなっていた。
  2. **リーシュ境界でのチャタリング**: ホーム復元力のnear/far切替が単一しきい値だったため、
     その距離ちょうどに選手が留まると無限に振動(見た目上は凍結と同じ)していた。
  3. **8方向量子化の合成に起因する足踏み**: 斜め方向の目標に対し、各項を先に8方向へ量子化
     してから合成する既存方式が軸ごとに打ち消し合い、ボール手前20〜30pxで永久に足踏み
     することがあった。「最終アプローチ」(近距離でボール引力を圧倒的優勢にする)で解消。

**検証方法も見直した**: 「統合テストは通るが実プレイでは壊れている」が2回続いたため、
無人(Direction8.None固定)のテストに加え、`tests/sim/humanLikeIntegration.test.ts`で
人間入力を模した決定論的な擬似ランダム入力(mulberry32流用、前進バイアス+たまに
他方向/停止/パス/キック)での統合テストを新設。3シード×前後半フル(21600tick)で
(a) Team Bがペナルティエリア到達 (b) 団子にならない、を検証。3シード中2シードで
実際にTeam Bが得点することも確認済み。詳細はコミット`2e87d48`参照。

**この3周目の修正もまだユーザーのブラウザ再確認を経ていない**。今回は特に、
「実プレイでのみ踏む」バグを潰したので、前回までより実際のプレイ感に近づいているはず。

## 0. 2周目の追記: CPU(Team B)関連バグ発見・修正

**1周目 (コミット`b337902`)**: 「敵がボールに寄ってこない」「敵がボールを持っても
攻めてこない」報告 → `computeNonControlledDirection`のホーム復元力が距離によらず
常にフル強度だったため非操作選手がホーム位置のすぐ外側で凍結するバグを修正
(距離に応じた2段階の復元力に変更)。

**2周目 (コミット`034abd9`)**: 1周目の修正確認後、「Team Bがハーフラインを越えて
攻めない」という新たな報告 → 調査の結果、2つの独立した原因を特定・修正:
1. **チームライン押し上げ/引き下げが無かった**: ユーザー要求どおり
   `computeLineAdjustedHomePosition`を新規実装。自チーム保持中はポジション別
   (GK≈0/DF小/FW大)にホームを押し上げ、相手保持中は控えめに引き下げる。
2. **【より重大】ゴールライン「詰み」バグを発見**: 転がるシュートが減速しながら
   ラインに近づく実際の物理挙動で、`clampToPitchBounds`のクランプ境界(ボール半径
   ぶん内側)と`detectBoundaryEvent`の判定しきい値(ラインちょうど)がズレていたため、
   残り速度がボール半径未満まで落ちた時点でクランプに押し戻され続け、**シュートが
   ゴールライン手前数pxで永久に静止し二度とゴール判定されない**という深刻なバグが
   あった。milestone3-4のテストがボールを境界の外側に直接置く形だったため、この
   自然な減速シナリオを一度も踏んでいなかった。修正: 判定しきい値をクランプ境界と
   完全に一致させた。**この2つ目のバグが「Team Bが得点しない」の主要因だった可能性が
   高く、人間のシュートにも同様に影響する(重要度が高い)**。

**この2周目の修正もまだユーザーのブラウザ再確認を経ていない**。

**未修正のまま報告する既知の別問題(今回は対象外)**: 調査中に「無操作(何も入力しない)を
長時間続けると、Team A自身の非操作選手がボールに触れてカーソル自動追従で操作対象が
そちらへスナップし、その選手が入力ゼロのまま静止し続けてボールごと止まる」という現象を
発見した。これはPhase 2で承認済みの「ボールを持った選手へ自動でカーソルが移る」設計と、
「本当に何も操作しない」という状況が組み合わさった時にのみ起きる別種の現象で、実際に
人間がプレイしていれば(何かしらボタンを押すため)ほぼ発生しない。今回のチームライン
修正の対象外として意図的に手を付けていない。もし実プレイ中に「急にキャラが動かなくなる」
瞬間が頻発するようなら、この現象の可能性があるので報告してほしい。



## 1. 完了した作業

- **Phase 0（基盤）完了・タグ`phase-0`push済み**: Vite+TS+Phaser3、固定タイムステップループ、決定論ユーティリティ、入力抽象化、縦スクロールカメラ、レーダー
- **Phase 1（ボールと1選手）完了・タグ`phase-1`push済み**: ドリブル・キック(速度軸+弾道軸)・ロングドリブル・ボール物理(重力/バウンド/転がり摩擦/疑似3D高さ)・カメラのボール追従切替・表示不具合(キャンバス中央寄せ)修正
- **Phase 2（11 vs 11とチームAI）完了・タグ`phase-2`push済み**: `GameState.players[]`(22人配列化)、フォーメーション定義(4-4-2/4-3-3/3-5-2/5-3-2)、非操作選手AI、カーソル切替+カーソルパス、キーパーAI、スライディングタックル、決定論/パフォーマンス強化
- **Phase 3（試合として成立させる）: 計画で定義した10副マイルストーンすべてに着手し、実装完了。タグ・pushは未実施（ユーザーの明示的な指示待ち、これまでと同じ運用）**
  - マイルストーン1-2: 半分(前後半)対応の幾何リファクタ(`teamDefendsNorth`一本化)+試合時計(`matchClock.ts`、`GameState.frame`から前後半/試合終了を導出)+前後半切替時のミラー配置キックオフリセット+フルタイム凍結
  - マイルストーン3-4: 境界越え検出(`sim/bounds.ts`、ゴールライン優先判定)+スローイン/ゴールキック/コーナーの自動リスタート(即座テレポート、試合停止演出なし)+得点判定(ゴール幅+クロスバー高さ判定)+スコアボードHUD(得点+前後半+経過分、画面固定表示)
  - マイルストーン5: オフサイドルール(`sim/offsideRule.ts`)、ON/OFF切替可能。キッカー以外の味方全員を判定対象にする設計(受け手を特定できないスルーパス主体のゲームのため、要プレイ感フィードバック)
  - マイルストーン6: CPU(Team B)攻撃AI(`sim/cpuAttackAI.ts`)、シュート/パス/ドリブルの優先順位判断、難易度3段階(`sim/difficultyConstants.ts`)、シュート照準ノイズでPhase 3初のRNG実消費
  - マイルストーン7: リプレイ入力ログ(`src/replay/`)、決定論を利用しseed+入力列だけで完全再生可能(ユニットテストで直接検証済み)
  - マイルストーン8: 効果音フック(`render/soundEvents.ts`/`SoundPlayer.ts`)、実アセットは未調達のためフックのみ(ユーザー承認済みのスコープ)
  - マイルストーン0: 試合前設定UI(`input/matchSetupOverlay.ts`、最小限)、難易度1/2/3キー+オフサイドOキートグル+Enter/Space開始
  - マイルストーン9: 前後半切替を跨ぐ長さの複合入力での結合スモークテスト(全マイルストーンの相互作用を確認)、決定論/パフォーマンスの最終検証
- テスト**224件**・`check:determinism`・`build`すべてgreen。`main`は9個のPhase 3コミット分だけ`origin/main`より進んでいる(タグ・push待ち)
- **重要: ブラウザでの目視確認は未実施**。今回のBrowser pane検証で、Phase 1/2の時から続く既知の環境問題（`document.hidden`が`true`のまま固定される）が、今回は**Phaser Sceneの`create()`自体が実行されない**という形で現れることを確認した（キャンバス要素は`Phaser.Game`起動時に作られるため存在するが、中身は背景色のままでピッチが一切描画されず、`#match-setup-overlay`のDOM操作も一切反映されない。ソースコード自体は`tsc -b`/テスト/ビルドすべて通過しており、配信されているコードも最新であることをネットワークリクエストの中身で確認済み）。**コードのバグではなく、このセッションのBrowser paneが可視状態にならない環境側の制約と判断**。ユーザー側での目視確認をお願いしたい

## 2. 進行中の作業と正確な現状

- Phase 3の10副マイルストーンはすべて実装・自動検証済みで`main`にコミット・push済み。
- Phase 4第1弾(マーク+サポートラン、5周目)は実プレイ確認済み。効果はあったが完全ではない
  （「少し効いている」「効いている」「多少解消した」という評価、上記0-00参照）。
- **7周目(リスタート猶予+イベントバナー、上記0-0000参照)も実装・自動検証済みで`main`に
  コミット・push済み。ただしユーザーの明示的な指示により、実プレイ確認は行わずに
  コミット・pushしている。次回セッション開始時点でこの変更はまだ一度もブラウザで
  確認されていない状態**。
- タグ`phase-3`の作成・pushはまだ行っていない（これまでのフェーズと同様、ユーザーからの明示的な指示を待つ）。
- **GitHub Pagesデプロイは完了・公開確認済み**（`https://appstockhub.github.io/vertical-striker/`）。

## 3. 次にやるべきこと（優先順）

1. **【最優先】ユーザーがブラウザで実際にプレイし、7周目(リスタート猶予+イベントバナー)の
   効果を確認**（`npm run dev`→http://localhost:5173、またはGitHub Pages公開版でも確認可能。
   設定UIで難易度/オフサイドを選んでEnterで試合開始、Z=B/X=A/C=Y/V=X/Q=L/E=R、矢印/WASDで移動）。
   特に確認してほしい点はセクション8のチェックリスト参照。
2. 確認結果に応じて調整（`teamAIConstants.ts`の`KICKOFF_GRACE_TICKS`/`RESTART_GRACE_TICKS`/
   `EVENT_BANNER_DURATION_TICKS`など）。
3. 5周目・Phase 3の個別手触り評価（オフサイド/CPU難易度/試合時間バランス/リスタート位置感覚）
   もまだ未着手。セクション8参照。
4. 確認後、問題なければ `phase-3` タグを打ってpush（ユーザー指示待ち、Phase 4着手済みのため優先度は下げても良い）
5. ゲームパッド実機確認（Phase 0から継続保留）

## 4. 未解決の課題・ユーザー判断待ち

- **【最新・最優先】7周目(リスタート猶予+イベントバナー、上記0-0000参照)はコミット・push済みだが、
  ユーザーのブラウザ実プレイ確認を一度も経ていない**。次回セッションはこの確認から開始する想定。
- 5周目(マーク+サポートラン)は実プレイ確認済みだが「効果はあるが完全ではない」評価。
  今回(7周目)の対応で改善しているはずだが、さらなる調整が必要になる可能性がある。
- オフサイド判定の「味方全員が対象」設計（`sim/offsideRule.ts`）: プレイ感次第で「キック方向のコーン内の味方だけを対象にする」への変更を検討する前提（計画に明記済み）
- CPU攻撃AIは攻撃行動のみ実装（タックル等の守備判断は対象外、ユーザーの要求文言「自律的な攻撃行動」に準拠した意図的なスコープ限定）
- CPUのシュートは常に溜め無し・低い弾道のグラウンダー固定（弾道バリエーションはPhase 4の手触り調整に先送り）
- フルタイム後のフローは未定義（最終スコア表示のまま入力無効化して停止するのみ。リマッチ等の導線が必要ならPhase 4以降で追加検討）
- Phase 3で新規追加した定数群(境界復帰位置・クロスバー高さ・難易度パラメータ・オフサイドの判定範囲等)はすべて仮値、プレイテストでの調整が必要
- ゲームパッド実機確認が継続的に未実施（Phase 0から持ち越し）
- Phase 1で対象外とした精密照準軸・カーブ/バックスピンの実装タイミング未決定

## 5. 重要な決定事項と理由

- **半分(前後半)の攻守方向は`formations.ts`の`teamDefendsNorth(team, half)`に一本化**。Phase 2まで散在していた`team===TeamId.A`のハードコード判定(`state.ts`のfacing初期値・`goalkeeperAI.ts`のセーブ方向符号等)をすべてここ経由に統一。後半キックオフ/得点後リセットで選手が逆向きのまま止まり無方向キックが自陣へ誤射するバグを計画段階で発見・未然に修正済み
- **得点は早期returnで完結**させ、選手移動処理まで進めない（古いボール位置ベースのAI目標が1tick混ざるのを防ぐため）。一方、非得点の境界復帰(スローイン等)は早期returnせず通常の選手移動処理まで継続する（試合停止の多フレーム演出を持たない設計方針、既存のファウル無しタックル設計と一貫）
- **リプレイログに`difficulty`/`offsideEnabled`を含める**（`seed`+`inputs`だけでは、再生時にこれらの設定が記録時と異なるとCPU判断/オフサイド判定が食い違い再現できないバグを計画段階で発見・修正済み）
- **CPU(Team B)専用の攻撃実行経路を新設**（Team Aの人間操作パス/キックの分岐とは完全に独立。`resolveCursor`の設計上Team Aが常にtouch-priorityを持てば即座に人間操作へスナップされるため、AIがボールを持ち続けるのは構造的にTeam Bだけ、という既存設計の帰結を利用）
- players[]の固定index規約・昇順走査+厳密な`<`比較のタイブレークは、オフサイド複数該当時の対象選手選定等、Phase 3でも一貫して踏襲
- 詳細な設計判断は各コミットメッセージ（`git log`、特に`323b757`〜`ed0ade9`のPhase 3系9コミット）とPlan Modeで作成した計画ファイルに集約済み

## 6. 変更ファイル一覧（Phase 3、このセッションで新規/変更したもの）

**新規**: `src/sim/{tacklePhase,kickoff,matchClock,bounds,boundsConstants,offsideRule,cpuAttackAI,difficultyConstants}.ts` / `src/replay/{replay,ReplayRecorder}.ts` / `src/render/{scoreboard,soundEvents,SoundPlayer}.ts` / `src/input/matchSetupOverlay.ts` / 対応する`tests/`配下のテストファイル一式

**変更**: `src/sim/{formations,state,teamAI,goalkeeperAI,goalkeeperConstants,ballPhysics,cursor,update}.ts` / `src/render/PitchScene.ts` / `index.html` / `src/style.css` / 既存テストファイル多数（half引数追加等）

## 7. 変更ファイル一覧（Phase 4第1弾: マーク+サポートラン、5周目で新規/変更したもの）

**新規**: `src/sim/{marking,supportRun}.ts` / `tests/sim/{marking,supportRun}.test.ts`

**変更**: `src/sim/{teamAI,teamAIConstants,update,state,cursor,cursorConstants,cpuAttackAI,formations}.ts`
（ホーム目標差し替えの統合・新定数・`GameState`への`lastTouchPlayerIndex`/`prevTouchPlayerIndex`追加・
カーソル切替ヒステリシスの相対比条件・CPUパスの相互ピンポン防止・`getOutfieldSlotDepthFrac`追加） /
`tests/sim/{matchSimulator,soccerSanity,teamAI}.test.ts`（新メトリクス2種・基準9/10・マトリクス2行追加・回帰テスト2件）

## 8. 実プレイ確認待ちタスク一覧（次回プレイ時にまとめて効率よく確認するためのチェックリスト）

上から優先順（直近の変更の効果確認 → 個別の手触り評価 → 未着手項目の再現条件ヒアリング）。
`npm run dev` → http://localhost:5173（またはGitHub Pages公開版）、
Z=B/X=A/C=Y/V=X/Q=L/E=R、矢印/WASD移動。

**A. 7周目(リスタート猶予+イベントバナー)の効果確認 — 最優先、まだ一度も未確認**
- [ ] キックオフ直後、敵側に先に触れられる不公平感が解消したか（リスタート猶予）
- [ ] ゴールキック直後にすぐ奪われる感覚が改善したか（リスタート猶予、
      ただし再現条件が未確認のままの対応なので完全解決の保証はない）
- [ ] サイドを割った時に「チームA スローイン」等のバナーが0.75秒程度表示されるか、
      表示位置・タイミングに違和感がないか
- [ ] GKが速いボールをキャッチした時に「チームA キャッチ！」バナーが表示されるか
      （自分のGKだけでなく、CPU(Team B)のGKがキャッチした時も表示されるか確認）

**B. 5周目(マーク+サポートラン+カーソル修正)の再評価 — 「効果はあるが完全ではない」との評価**
- [ ] サポートラン「少し効いている」から、7周目の変更後も含めてさらに効果を感じるか
- [ ] マーク「効いている」の継続確認
- [ ] カーソルの暴れ「多少解消した」→ 完全に解消したか、まだ残る場面があるか

**C. Phase 3の個別手触り評価 — 見た目・基本動作は確認済みだが評価は未着手**
- [ ] オフサイド判定「キッカー以外の味方全員が対象」設計の体感
      （無関係な選手の位置で意図しないプレーが止まる違和感が無いか）
- [ ] CPU難易度3段階(イージー/ミディアム/ハード)の手応えの違い
- [ ] `HALF_DURATION_FRAMES`(1ハーフ3分、仮値)を含む試合時間バランス
- [ ] ゴールキック/コーナー/スローインの復帰位置の距離感

**D. 継続保留（Phase 0から）**
- [ ] ゲームパッド実機接続確認（ボタン配置オーバーレイ表示・8方向入力・SFC論理ボタン
      マッピングが正しいか）

## 9. 変更ファイル一覧（6周目: GitHub Actions + Pagesデプロイ）

**新規**: `.github/workflows/deploy-pages.yml`

**変更**: なし（`vite.config.ts`は既存の`base: './'`のまま、変更不要と判断）

## 10. 変更ファイル一覧（7周目: リスタート猶予 + イベントバナー、このセッションで新規/変更したもの）

**新規**: `src/render/eventBanner.ts` / `tests/sim/restartGrace.test.ts` / `tests/render/eventBanner.test.ts`

**変更**: `src/sim/{state,teamAI,teamAIConstants,update}.ts`
（`GameState`への`restartGraceTeam`/`restartGraceTicksLeft`/`lastEvent`追加・
`computeChaseRightIndices`への`suppressedTeam`引数・primary専用ボールデッドゾーン(4→9px)・
追跡権メンバーシップの距離バケット拡大(48→64px)） /
`src/render/{PitchScene,SoundPlayer,soundEvents}.ts`（イベントバナーHUD統合・`GkCatch`音声フック） /
`tests/{sim/teamAI,render/soundEvents}.test.ts`（回帰テスト追加）
