{
  "name": "vertical-striker",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "check:determinism": "node scripts/checkDeterminism.mjs",
    "sim:report": "vitest run tests/sim/matchReport.test.ts --reporter=verbose",
    "view:metrics": "vitest run tests/render/viewMetrics.test.ts --reporter=verbose",
    "sim:sanity": "vitest run tests/sim/soccerSanity.test.ts --reporter=verbose"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^8.2.1",
    "vitest": "^4.1.10"
  }
}
