# CRITIC.md — 合否判定の基準書

作成: 2026-08-17（24周目、凍結解除・自律開発モード移行時）。
このファイルは**批評役（critic サブエージェント）が実装の合否を判定するための基準**である。
実装者（メインセッション）はこの基準で**自己採点してはならない**。判定は必ず批評役が行う。

対になる文書:
- `docs/parity-targets.md` — 原作実測値と許容範囲（数値ゲートの正本）
- `docs/autonomous-log.md` — 周次の進捗と判定結果の記録

---

## 原則1: テストgreenは完成を意味しない

**前例**: 12周目まで全374テストgreenのまま、実プレイは「キックが反応しない・スライディングも
チャージもない・キーパーがキャッチしない」遊べない状態だった。16周目には全テストgreenのまま
**1試合の74%がセットプレーのロックで凍結**していた。

**判定への適用**:
- 「全テストが通った」は合格の**必要条件であって十分条件ではない**
- 合格には、その変更が対象とする挙動の**実測値**（シナリオテストの出力・キャプチャ・
  シミュレーター統計のいずれか）が提出されていること
- 「〜のはず」「実装した」という言葉だけの主張は**証拠不採用**として差し戻す

## 原則2: 主張は必ず実測値とキャプチャで裏付ける

**前例**: 13周目の4件の不具合はすべて「先に計測して数値で根本原因を確定させてから」直した
（例: 「ドリブル120tick中68tickでボールを見失う」「CATCH_MAX_SPEED(7) < STRONG_KICK_SPEED(9)
なので全シュートが弾かれる」）。22周目のゴール高さ修正も「実測12.36:1 → 目標3:1 → 修正後3.04:1」
と数値で閉じた。

**判定への適用**:
- 修正の前後で同じ指標を測り、**before/after の数値**が並んでいること
- 見た目の変更はPNGキャプチャ（`scripts/shotSink.mjs` / `__vsDebug.renderAt`）の提出が必須
- 数値が目標に届いていない場合、「近づいた」は不合格。許容範囲内のみ合格

## 原則3: 順序の原則 — テンポ → 操作 → AI

**理由**: テンポ（速度の絶対値）を変えると全AIしきい値（プレス距離・マーク距離・タックル
間合い）とプレイアビリティテストの前提が変わる。操作が信頼できない状態ではAIの良し悪しを
判定できない。土台から順に固めないと手戻りが増える。

**判定への適用**:
- テンポ未確定のまま操作の細部を調整する変更は、根拠がない限り差し戻す
- AI調整はテンポと操作の両ゲート合格後のみ合格対象とする
- 逆順の変更が必要な場合は、理由が `docs/autonomous-log.md` に記録されていること

## 原則4: 北極星 — スルーパスが通る快感

このゲームの存在理由は「シンプル操作 × フル判断 × 俯瞰視点」で、**スルーパスが通った瞬間が
最高に気持ちいいこと**。エイムアシストは入れない（照準スキルを薄めない）。

**判定への適用**:
- 判断が割れる調整は「この変更はスルーパスを狙う楽しさを強めるか弱めるか」で裁く
- 照準を自動化・補正する変更は、ユーザーの明示指示がない限り不合格

## 原則5: 一度に複数変更しない

**前例**: カーブ・リフティング実装時、「物理/挙動の変更が試合全体のバタフライ効果で既存AIの
潜在的振動ケースを踏む」regressionが繰り返し発生した。複数の変更を同時に入れると、どの変更が
どの回帰を起こしたか切り分けられない。

**判定への適用**:
- 1コミット = 1つの挙動変更。物理定数の変更とAIしきい値の変更が不可分な場合のみ
  セットで1コミットとし、その理由を記録すること
- 回帰が出た場合、「観戦シミュレーターのoscillatingPlayers → 該当tick周辺のトレース →
  原因切り分け」の手順を踏んだ記録がなければ、scriptSeed変更による回避は不合格

## 原則6: 決定論厳守

将来のネット対戦とリプレイ共有はすべて決定論の上に成り立つ。

**判定への適用**:
- `npm run check:determinism` green は**全サイクル必須**（例外なし）
- `src/sim/` への変更に浮動小数・`Math.random()`・フレーム依存処理が混入していたら
  他が全部greenでも不合格
- 描画層（`src/render/`）の変更が `src/sim/` に波及していないこと（diffで確認）

## 原則7: 数値ターゲットだけに頼らず、画も見る

**前例**: 21周目、数値ターゲット（輝度など）は達成していたのに「縞の向きが原作と逆」という
見た目レベルの誤りが実機確認で発覚した。数値は必要条件で、画としての正しさは別に確認が要る。

**判定への適用**:
- 見た目に関わる変更は、数値の合格に加えてキャプチャの目視確認を批評役自身が行う
- 原作との対比が問題になる項目は、原作動画のフレームと並べて比較する

---

## 判定手順（批評役が毎サイクル実行すること)

1. **証拠の検収**: 実装者の提出物（変更ファイル一覧・テスト結果・実測値・キャプチャ）が
   上記原則の要求を満たしているか確認。欠けていれば**判定せず差し戻す**
2. **ゲート照合**: `docs/parity-targets.md` の該当行と実測値を突き合わせ、許容範囲内かを判定
3. **回帰確認**: 全テスト・`check:determinism`・観戦シミュレーター統計・既存の合格済み
   シナリオ（ラチェット）がすべてgreenであることを**自分で実行して**確認する
   （実装者の報告を信用しない）
4. **判定の記録**: PASS / FAIL(理由と差分) / 差し戻し(不足証拠) のいずれかを
   `docs/autonomous-log.md` に記録する

## 合否の定義

- **PASS**: 対象ゲートが許容範囲内 + 全回帰green + 証拠完備
- **FAIL**: いずれかのゲートが範囲外。差分の数値と、考えられる原因を添えて返す
- **差し戻し**: 証拠不備。何が足りないかを列挙して返す（判定はしない)
