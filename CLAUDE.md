# CLAUDE.md — 縦スクロールサッカーゲーム（仮称: Vertical Striker）

## プロジェクト概要

SFC『スーパーフォーメーションサッカー』の競技性（縦スクロール視点＋シュートコースを自分で狙う照準スキル＋フォーメーション戦術）を精神的に継承する、完全オリジナルのブラウザゲームを開発する。

- 名称・チーム名・選手名・グラフィック・音声は一切流用しない（完全オリジナル素材）
- ゲームメカニクス（操作体系・視点・ルール）の再現は自由
- 最終目標: Web公開 → 非同期対戦 → リアルタイムP2P対戦 → Steam
- 収益化はしない。拡散性と継続性を最優先

## 技術スタック

- 言語: TypeScript
- レンダリング: Canvas 2D（Phaser 3 を採用。物理は自前実装、Phaserは描画/入力/シーン管理のみ使用）
- 入力: Gamepad API（最優先）+ キーボード。初日からパッド対応
- ビルド: Vite
- 将来のロックステップ同期に備え、試合ロジックは完全決定論で書く:
  - 乱数は seed 付き PRNG（mulberry32等）のみ。Math.random() 使用禁止
  - 物理は固定小数点または整数演算（座標は 1/256 ピクセル単位の整数）
  - ゲームループは固定タイムステップ 60fps（描画と更新を分離）
  - 入力→状態遷移が純関数になるよう、UpdateState(state, inputs) -> state の構造を守る

## 段階開発計画（この順で実装。各フェーズ完了ごとに動くものをコミット）

### Phase 0: 基盤（1-2日）

- 固定タイムステップのゲームループ、決定論物理の骨格、Gamepad/キーボード入力抽象化レイヤー
- 縦長ピッチ（比率 縦:横 ≈ 1.5:1）の縦スクロールカメラ（ボール追従＋進行方向先読みオフセット）
- レーダー（全体ミニマップ）を初日から実装。縦視点の視野の浅さを補う最重要UI

### Phase 1: ボールと1選手（2-3日）

- ドリブル（ボールが足元に吸着しすぎない。触れると少し前に転がる）
- キック実装（下記「操作仕様」参照）: 方向同時押しで強キック、押下時間で弾道高さ
- ボール物理: 転がり摩擦、バウンド、高さ（z軸）を持つ疑似3D

### Phase 2: 11 vs 11 とチームAI（1週間）

- フォーメーション定義（4-4-2, 4-3-3, 3-5-2, 5-3-2 など）
- 非操作選手AI: 「ホームポジションへの復元力 + ボール位置への引力 + オフサイドライン意識」の重み付きベクトル合成
- カーソル切替（操作選手の変更）、カーソルパス（受け手マーカー表示）
- キーパーAI: シュートコースへの反応。反応速度と守備範囲をパラメータ化
- スライディングタックル（ファールなし。読み勝ちで奪える設計）

### Phase 3: 試合として成立させる（1週間）

- ルール: ゴール、スローイン、ゴールキック、コーナー、オフサイド（トグルでON/OFF）
- 前後半・時間・スコア、リプレイ用の入力ログ記録（決定論なので入力列だけで再生可能）
- CPU対戦の難易度3段階
- サウンド（効果音は自作 or CC0素材）

### Phase 4: 手触り調整＋選手個性（2週間、反復作業）

- 選手パラメータ: 走力 / キック力 / テクニック / タックル / キーパー能力
- チーム16種（架空国家 or 架空クラブ。名称は完全オリジナル）
- ゴール後の自動リプレイ再生 → GIF/動画書き出し → Xシェアボタン（拡散の心臓部）

### Phase 5: ローカル2P

- Gamepad 2台の同時入力対応（入力抽象化レイヤーができていればほぼ無料）

### Phase 6: オンライン

- 6a: 非同期対戦（相手の編成・戦術設定を持つAIと対戦、結果をサーバー記録）
- 6b: リアルタイムP2P（WebRTC DataChannel + 決定論ロックステップ。シグナリングは自前の小型サーバー）

## 操作仕様（SFC版フォーメーションサッカーの操作トレース）

### 確定仕様（任天堂公式取扱説明書 [CLV-P-VADFJ.pdf](https://www.nintendo.co.jp/clvs/manuals/ja/pdf/CLV-P-VADFJ.pdf) の記載内容で確定。以後これを一次資料とし、下記の内容が最終的な正）

ボタン名は SFC 配置（下=B, 右=A, 左=Y, 上=X）で記述。Xbox系パッドでは B→A, Y→X, A→B に自動マッピング。

操作の意味は「ボールの状態」で変わる、原作の文脈依存設計。3つの文脈がある:

- **ボール近く（非保持）**: ボールが誰にも完全に保持されていない状況（こぼれ球・浮き球への飛び込み等）
- **自分が保持**: 自チームがボールを持っている
- **相手が保持**: 相手チームがボールを持っている（ドリブル中等）

| ボタン | ボール近く（非保持） | 自分が保持 | 相手が保持 |
|---|---|---|---|
| +字 | ゼッケン表示選手を移動 / キック方向決定 | ドリブル方向 | ゼッケン表示選手を移動 / タックル方向 |
| Y | 緩い弾道のキック | 矢印カーソル表示中の選手へパス | ショルダータックル |
| B | 鋭い弾道のキック | +字方向へキック | スライディングタックル |
| A | スライディング | +字方向へパス | （記載なし） |
| L / R / START | パス先の矢印カーソルを切り替え（全文脈共通） | | |
| SELECT | ポーズ | | |
| X | 未使用 | | |

#### 移動系

- 十字キー/左スティック: ゼッケン表示選手の移動・ドリブル方向（8方向）
- ドリブル中に L/R 押し続け: **【続編仕様で置き換え】** 旧仮実装は「当プロジェクト独自仕様」だったが、
  続編の公式説明書で正式な原作機能「蹴り出しドリブル」だったと判明した。詳細は下記
  「続編仕様: 蹴り出しドリブル」節を参照(仕様が異なるため続編側を採用する)

#### キック — 弾道はY(緩い)/B(鋭い)の2択、方向は+字、コースは位置取りで狙う

- 自分が保持している時: **B = +字方向へ鋭いキック**（シュート/強いパス用）。**Y = 矢印カーソル表示中の受け手へ自動パス**（カーソルパス、下記参照）
- ボールが近いが誰も保持していない時（こぼれ球・浮き球）: **B = 鋭い弾道でのファーストタッチキック**、**Y = 緩い弾道でのファーストタッチキック**。方向は+字で決める
- **弾道の決定方式（重要な訂正、さらに続編仕様で再訂正）**: 旧CLAUDE.mdは「Bの押下時間」で弾道(高さ)が
  変わると記述していたが誤りで、初代は**「+字ボタンを押してからBを押すまでの間隔」**で決まる、と一度
  訂正した。しかし**続編の公式説明書では「キックボタンの押下時間」方式が確定**しており、初代と続編で
  仕様が異なる箇所は続編を採用する方針のため、**最終的には「押下時間」方式を実装する**
  (詳細は下記「続編仕様: ボールの強さ・弾道の変化」節)。間隔方式は初代の記録としてのみ残す
- シュート専用ボタンは無い。シュート＝ゴールへ向けた+字方向+B。コースは位置取り＋方向入力の精度で狙う（照準スキル）
- **回転軸（カーブ）は続編の公式説明書で存在が確定した**。旧CLAUDE.mdの「公式説明書に記載が無い当
  プロジェクト独自仕様」という位置づけは誤りだったため撤回する。詳細は下記「続編仕様: カーブ」節
  (キック"直後"ではなく"キックボタンを押した瞬間に同時"に+字を入れる、という続編の記述に合わせて
  実装を修正する)
- **L/R は精密照準（8方向解禁）ではない**が、続編の「シフトキック」が近い機能を持つと判明した
  （4方向基準のキック方向に対し、L/Rでキック時に左右へオフセットをかける）。旧旧CLAUDE.mdの
  「精密照準」という初期の推測は、原作(初代)の仕様としては誤りだったが、続編の実在機能を先取り
  していた形になる。詳細は下記「続編仕様: シフトキック」節。初代のL/R/STARTの役割
  「パス先の矢印カーソルの切り替え」は続編でも共通操作として残るため、この役割自体は変わらない
  (シフトキック/蹴り出しドリブルは「キック中/ドリブル中にL・Rを押しているかどうか」で判定する
  文脈依存の追加機能であり、カーソル切替を置き換えるものではない)

#### カーソルパス（Yボタン、自分が保持している時）

- パス可能な味方に「↓」矢印マーカーが表示され、Yでその選手へ自動パス
- **L/R/START でその矢印カーソル（パス先候補）を切り替えられる**（複数の受け手候補がいる時に選び直す操作。公式説明書で確定、旧CLAUDE.mdには記載が無かった新情報）
- 狙い撃ちのB（自由だが精度は自分次第）と、自動補足のY（確実だが受け手は選べない、ただしL/R/STARTで候補を選び直せる）の使い分けが攻撃のリズムを作る

#### 守備 — タックルは1種類ではない

- **相手がボールを保持している時**: B = スライディングタックル、Y = ショルダータックル。2種類ある（旧CLAUDE.mdはBの文脈的再利用のみを仮定しており、ショルダータックルは未記載だった）
- **ボールが近いが誰も保持していない時（こぼれ球）**: A = スライディング（ボールへ飛び込んで奪う）
- 相手の背後からドリブル方向と同方向に滑ると、ジャンプでかわされても着地際で奪える（読み合いの成立、当プロジェクトの既存設計として維持）
- **ファウル判定について**: 公式には以下の仕組みがあるが、当プロジェクトは現行の「ファウル無し」を意図的なゲームデザイン（読み合いで奪える設計）として採用しており、この節は原作の忠実な記録に留める。**実装するかどうかは要ユーザー判断**:
  - ショルダータックル（Y）でレッドカード・退場が発生しうる
  - 反則退場は1チーム4名まで。自軍が7名になるとショルダータックル自体が不可になる
  - 負傷退場もあり、欠員補充が可能

#### キーパー操作 — AUTO/MANUAL選択式

- **試合前に AUTO / MANUAL を選択できる**（公式仕様。旧CLAUDE.mdの「ボールが近づくと自動的にマニュアルへ移行」は不正確だったため訂正する）:
  - AUTO: COMが自動反応・キャッチする。ただし**捕球後は一時的にMANUAL操作に移行する**（蹴り出し方向を自分で選べるようにするため）
  - MANUAL: 常に自分でキーパーを操作する
- +字 = 移動・ジャンプ方向
- Y = ジャンプしながらキャッチ: ボールを確保できるが守備範囲（飛びつき距離）が短い。速いボールはキャッチを試みても弾いてしまう
- B = ジャンプしながらパンチング: 飛びつき距離が**キャッチの1.5倍**（公式説明書で確定した具体的な倍率、旧CLAUDE.mdは「長い」とだけ記述していた）。必ず弾く（こぼれ球が発生する）

つまり「確実に届くならY、ギリギリならB、ただしBのこぼれ球は詰められるリスク」というリスクリターン構造。シューター側の「速いボールはキャッチ不能」仕様と対になっており、強シュート→弾く→詰める、の攻防が生まれる

#### セットプレー（原作の挙動）

- **スローイン**: 相手が蹴ったボールがタッチラインを越えたら自軍のスローイン。B = 3方向、Y = パス先の矢印カーソル選手へ投げる
- **コーナーキック**: 相手が蹴ったボールが相手ゴールラインを越えたら自軍のコーナーキック（8方向）
- **ゴールキック**: 相手が蹴ったボールが自軍ゴールラインを越えたら自軍のゴールエリア内から自軍のゴールキック
- （旧CLAUDE.mdの「通常キックと同一体系」という仮定は、リスタート発生条件・方向操作の粒度が通常時と異なる可能性を見落としていた。詳細な原則はセットプレーの実装ギャップ節を参照）

#### ゴール仕様（検証済みの攻略セオリー。ゲームバランスの正解値として利用）

- ゴール隅への速い強キックはキーパーが反応できない（＝コースを狙う照準に報酬がある）
- 斜め45度からの角度をつけた強キック/浮き球でゴール隅を狙うのが基本セオリー
- キーパーを45度ドリブルでかわして裏からシュートが成立する

### 続編仕様（シリーズ後期作品の公式取扱説明書で判明。実装優先度の高い拡張仕様）

初代の確定仕様を土台に、後期作品(以下「続編」)の公式取扱説明書で新たに判明した拡張仕様。
**初代と続編で仕様が異なる項目は続編を採用する**方針(ユーザー指示)。上記の初代確定仕様の
各所に「続編仕様で置き換え」の注記を入れてある。

#### ボタン配置（続編版。初代からの変更点に注意）

ボタン名は引き続きSFC配置(下=B, 右=A, 左=Y, 上=X)で記述。Xボタンが本格的に使われるように
なるため、キーボード/ゲームパッド割当を新規に追加する必要がある(初代は未使用ボタンだった)。

| ボタン | ルーズボール時 | ボールキープ時 | 相手ボール時 |
|---|---|---|---|
| +字 | 選手の移動 | 選手の移動 | 選手の移動 |
| B | シュート、クリア | シュート | ショルダーチャージ |
| Y | パスカーソル先へのパス | パスカーソル先へのパス | スライディングタックル |
| A | スライディング、ジャンピングヘッド、ダイビングヘッド | 進行方向へのパス | スライディングタックル |
| X | ロビングのパス | ロングフィード、センタリング | ショルダーチャージ |
| START | (未使用) | オフェンスラインを下げる | ディフェンスラインを上げる |
| L/R | パスカーソルの切り替え(共通、下記参照) | 同左 | 同左 |
| SELECT | ポーズ(ポーズ中Bで画面下インフォメーション切替) / アウトオブプレー中はチーム情報画面 | 同左 | 同左 |

セットプレー(ゴールキック/コーナーキック/フリーキック)はガイドバー方式(下記参照)、
スローインは別体系(下記参照)。

- 相手ボール時、B/Aがどちらもスライディングタックル、X/Bがどちらもショルダーチャージという
  「同じ効果に2ボタン割り当て」が続編の設計(初代はB=スライディング/Y=ショルダーの1対1対応
  だったが、続編ではA/BどちらでもスライディングOK・X/Bどちらでもショルダーチャージ可、と
  やや冗長になっている)。実装は「A or B → スライディング」「X or B → ショルダー」の
  OR判定でよい
- ボールキープ時のA(進行方向パス)は初代の確定仕様と同じ役割を引き継ぐ

#### セットプレー: ガイドバー方式（ゴールキック・コーナーキック・フリーキック）

初代の「+字で方向決定→B/Yでキック実行」という一体化した操作から変わり、続編では**方向決定と
キック実行が分離**する:
- +字: ガイドバー(狙う方向のインジケーター)を動かして方向を指定する
- B: ガイドバー方向を確定し、低い弾道のキック
- Y: ガイドバー方向を確定し、パスカーソル先の選手へパス
- A: ガイドバー方向を確定し、ガイドバー方向へのパス
- X: ガイドバー方向を確定し、高い弾道のキック

このガイドバーは「精密な方向決定を、実際にキックを打つ前に確認しながら行える」仕組みで、
通常プレー中の即時キックより照準の自由度が高い(セットプレーらしい"間"のある操作性)。

#### スローイン: 専用体系（初代のBキック体系から分離）

- +字: 方向指定
- B: +字ボタンの入力方向へスローイン
- Y: パスカーソル先の選手へスローイン
- A/X: 未使用

#### 蹴り出しドリブル（初代の「L/R押し続けロングドリブル」を置き換える正式仕様）

ボールキープ時に **L と R を同時押し**すると、選手がボールを少し前方へ蹴り出す。その後
L か R の片方を押したまま選手がボールに再び触れると、同様に蹴り出す。これを繰り返すことで
通常のドリブルとは異なる「蹴り出しドリブル」ができる:
- 相手のスライディングをかわせる（ボールが足元から離れているため、タックル対象を空振りさせやすい）
- 走力の高い選手は通常ドリブルより速くボールを運べる
- ただし選手の能力次第ではボディバランスを崩すことがある(仮: 当プロジェクトでは選手個性
  パラメータ未実装のPhase4前半時点では、この不安定化要素は後回しにしてよい)

実装方針: 既存の`LONG_DRIBBLE_PLAYER_SPEED_FIXED`/`LONG_DRIBBLE_TOUCH_SPEED_FIXED`
(`src/sim/ballConstants.ts`)は「常にL/R押しっぱなしで速度up」という初代仮実装の名残りなので、
「L+R同時押しで蹴り出し(ボールがドリブル半径外まで飛ぶ)→片方押しっぱなしで継続」という
離散的なトリガー式に作り替える(`src/sim/dribble.ts`)。

#### シフトキック（初代「精密照準」の実体、L/R役割の拡張）

キック・ヘディング等を行う瞬間に L または R を押していると、通常の4方向基準のキック方向に
対し、それぞれ左/右方向へオフセットをかけた向きにボールを送り出せる。CLAUDE.mdの旧い
「L/R押しながらキックで4方向→8方向に解禁」という記述(攻略資料ベースの誤情報として初代仕様
からは撤回済み)は、実は続編のこの機能を先取りした記述だったとわかった。

実装方針: 8方向のキック方向候補のうち、+字で選んだ基準方向(4方向: 上下左右)に対し、
L押しなら反時計回り、R押しなら時計回り隣接の斜め方向へシフトする(例: 基準=上, R押し→
右上にシフト)。既存の「L/R押下中のみ8方向照準UI(薄い方向ガイド)を表示」という初代計画時の
アイデアはこの機能の可視化に転用できる。

#### カーブ

ボールをキックする瞬間(**キックボタンを押した"後"ではなく"同時に"**)+字を入れると、蹴り出された
ボールが入力方向へ曲がる。初代CLAUDE.mdが想定していた「キック後の後入力ウィンドウ」方式
(キック確定後Nフレーム受け付ける)とは入力タイミングの前提が異なる点に注意し、実装を修正する:
キック入力(B/A/X等)と+字方向入力が「同フレームまたはキック入力直前の短いウィンドウ内」に
揃った時にカーブが発生する、という判定に切り替える。バックスピン(初代CLAUDE.mdが独自追加して
いた「曲がった後さらに逆方向入力で減速」)は続編の説明書に記載が無いため、当面は仕様として
保留(削除はしない、実装するかはプレイ感次第)。

#### ボールの強さ・弾道の変化（初代の「間隔方式」を置き換える）

キックボタンを押す長さで、ボールの強さと弾道の両方が変わる。基本的に押す長さがそのまま
ボールの強さになる。ただし**Bのシュートは強く蹴ると高い弾道になり、ゴールバーを越えてしまう
ことがある**(=「強く蹴りすぎるとふかす」というリスクがボタン単位で存在する、独特のバランス
設計)。実装方針: 既存の`kickChargeFrames`(押下フレーム数を数える仕組み自体)はそのまま使え、
「+字→Bの間隔で弾道決定」という初代仕様として実装しかけていた部分を「押下時間で強さ+弾道が
連動して変化(Bだけは強さに応じてバー超えリスクも上がる)」というモデルに差し替える。

#### リフティング

選手が急に方向転換すると、動きが一瞬止まる(ターンアクション)。ボール保持者がこのターン
アクション中にキックボタンを押すと、リフティングを行い自分の頭上にボールを軽く蹴り上げる
(浮き球状態にして保持を継続する動作)。実装方針: 既存の「急な方向転換」を検出する仕組みが
現状無いため、新規に「直前フレームの移動方向と今フレームの移動方向が正反対(またはそれに近い)」
を判定条件として追加する必要がある。優先度は低め(⑥、下記実装優先順参照)。

#### 1、2パス（ワンツー）

ボールを受ける瞬間に A または Y を押していると、ボールを受けた選手がそのまますぐにパスを
出す(ターンやトラップの動作を挟まない即時リターン)。Aなら+字入力方向へ、Yならパスカーソル
先の選手へ。実装方針: 「ボールを受けた瞬間」= touch-priorityが新しい選手へ切り替わった
その1tick、というタイミングで判定できる(既存の`lastTouchPlayerIndex`変化検出と同種の仕組み)。

#### ライン操作とオフサイドトラップ（戦略性の核心、STARTボタン新設）

- 相手ボール時のSTART: ディフェンスラインを上げる(オフサイドトラップを仕掛ける)
- ボールキープ時のSTART: オフェンスラインを下げる(相手のオフサイドトラップを回避する)
- オフサイドルール自体は初代確定仕様のままON/OFFトグル可能(実装済み、`sim/offsideRule.ts`)

実装方針: 既存のチームライン押し上げ/引き下げ機構(`computeLineAdjustedHomePosition`、
`src/sim/teamAI.ts`)は「保持チームか否か」で自動的にラインを調整する仕組みだが、これに
「プレイヤーが手動で追加オフセットをかけられる」レイヤーを足す形で統合する
(`GameState`に手動ラインオフセット値を持たせ、START押下のedgeで段階的に増減させる想定。
既存のライン量子化(`LINE_FOLLOW_GRID_FIXED`)やオンサイドクランプ(`ONSIDE_HOME_MARGIN_FIXED`)
との相互作用に注意し、既存の振動対策を壊さないよう慎重に設計する)。

#### 実装優先順（ユーザー指示）

①シフトキック → ②蹴り出しドリブル → ③カーブ → ④ライン操作/オフサイドトラップ →
⑤ワンツー → ⑥リフティング。各機能ごとにコミットを分け、都度観戦シミュレーターで回帰確認する。

## 実装ギャップ（公式説明書での仕様確定を受けた、現行実装との差分。優先度はユーザー判断）

今回の仕様確定より前の実装は、攻略資料ベースの推測で作られている箇所が複数ある。以下は
2026-08-15時点で判明している差分（優先度はユーザー指示に基づく）:

1. **[最優先・対応済み]** セットプレーのリスタート原則: キッカーが攻撃方向を向いて開始する/
   相手が蹴るまでボールに触れられない、が守られていなかった。`GameState.setPieceLock`
   (`src/sim/state.ts`)として「キッカーが実際にボールを動かすまで解除されない」状態ベースの
   ロックへ再設計し、touch-priorityのteam制限(`findTouchPriorityPlayer`の`restrictToTeam`)と
   組み合わせて対称に3種類(スローイン/ゴールキック/コーナー)すべてに適用した。詳細は
   HANDOFF.md参照
2. **[次点]** 入力レスポンス: ボタンを押してからキックが発生するまでの体感遅延・視覚フィード
   バック不足の調査。対応状況はHANDOFF.md参照
3. **[3番目、続編仕様確定によりスコープ更新]** ボタン配置の原作準拠化: 当初は初代仕様
   (A=+字方向パス、Yの文脈依存化、Bの弾道判定を「押下時間→間隔判定」)を想定していたが、
   続編の公式説明書確定によりBの弾道は「押下時間」方式へ**差し戻し**(間隔判定は実装しない)、
   X/STARTボタンも新たに使うボタン配置全体を続編仕様で実装する。詳細は上記「続編仕様」節、
   実装優先順は①シフトキック②蹴り出しドリブル③カーブ④ライン操作⑤ワンツー⑥リフティング。
   対応状況はHANDOFF.md参照
4. 守備時カーソル切替: 公式にはボタンでの手動切替が無い(「ゼッケン表示選手」への自動追従の
   み)。旧仮実装の「非保持中はY=手動カーソル切替」は誤りだったと判明(Yは相手保持時は
   ショルダータックルに割り当たるため)。優先度3の作業に含めて修正する
5. GK AUTO/MANUAL事前選択方式: 公式は試合前の選択式だが、現行実装は「ボール近接で自動的に
   マニュアル移行」方式。未着手、優先度未定
6. ファウル/退場システム: 未着手。実装要否はユーザー判断待ち(上記「守備」節参照)

## 独自仕様（公式説明書に記載が無く、当プロジェクトが追加した要素。実機データでの裏付けは無い）

- **【続編仕様確定により撤回】** ロングドリブル(L/R押し続け)とカーブは、かつて「当プロジェクト
  独自仕様」に分類していたが、続編の公式説明書でそれぞれ「蹴り出しドリブル」「カーブ」という
  正式な原作機能だったと判明した。上記「続編仕様」節を参照(この節からは削除)
- バックスピン(カーブ後、逆方向入力でさらに減速・戻り回転)は続編の説明書にも記載が無い。
  実装するかはプレイ感次第の当プロジェクト独自パラメータとして保留のまま残す
- 蹴り出しドリブルの「選手のボディバランスを崩すことがある」という能力依存要素は、選手個性
  パラメータ(Phase 4後半、走力/キック力/テクニック等)が未実装の現時点では実装を見送る。
  実装時期が来たら「実機で正解値を測る」対象ではなく、プレイフィールで調整する当プロジェクト
  独自の数値パラメータとして扱う

## 設計上の絶対原則

- 照準スキルを薄めない: エイムアシストは入れない。ゴール隅に狙って決めた快感がこのゲームの存在理由
- 視野の浅さはレーダーで補う: 縦スクロールの構造的弱点。レーダーの視認性・情報量には妥協しない
- 1ボタン多表現: ボタンを増やすのではなく、方向同時押し・押下時間・状況文脈で表現力を出す（SFC設計思想の継承)
- 決定論を壊さない: 将来のネット対戦とリプレイ共有はすべて決定論の上に成り立つ。浮動小数・Math.random・フレーム依存処理の混入はレビューで必ず弾く
- 60fpsを死守: 手触りのゲーム。パフォーマンス劣化する実装は却下

## リポジトリ運用

- GitHub: appstockhub 配下に新規リポジトリ
- /handoff + /catchup ワークフロー準拠（HANDOFF.md 更新を各セッション末に実施)
- Phase完了ごとにタグを打つ。Phase 1 完了時点から GitHub Pages でプレイ可能にする

## 最初のタスク

Phase 0 を実装せよ:

- Vite + TypeScript + Phaser 3 のプロジェクト scaffolding
- 固定タイムステップ 60fps ループ（更新と描画の分離）
- seed付きPRNG と整数座標系のユーティリティ
- Gamepad API + キーボードの入力抽象化（SFC論理ボタン B/Y/A/X/L/R + 8方向にマップ、接続時にボタン配置オーバーレイ表示）
- 縦長ピッチの縦スクロールカメラとレーダーの骨格
- 動作確認用: 十字キーで動く仮選手1体とボール1個

完了条件: ブラウザでパッドを繋いで仮選手が8方向に動き、レーダーに位置が反映されること。
